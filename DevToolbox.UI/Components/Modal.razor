@if (IsVisible)
{
    <!-- Backdrop -->
    <div class="modal-backdrop" @onclick="Close"></div>
    
    <!-- Modal Container -->
    <div class="modal-container" @onclick:stopPropagation="true">
        <div class="modal-content @SizeClass">
            <!-- Header -->
            <div class="flex items-center justify-between p-6 border-b border-dark-border">
                <h2 class="text-xl font-semibold text-dark-text">@Title</h2>
                <button type="button" 
                    class="btn-ghost p-2 -mr-2 hover:bg-dark-surface-hover rounded-lg transition-colors"
                    @onclick="Close">
                    <i class="bi bi-x-lg text-dark-text-muted"></i>
                </button>
            </div>
            
            <!-- Body -->
            <div class="p-6">
                @ChildContent
            </div>
        </div>
    </div>
}

@code {
    [Parameter] public string Title { get; set; } = "";
    [Parameter] public RenderFragment? ChildContent { get; set; }
    [Parameter] public bool IsVisible { get; set; }
    [Parameter] public EventCallback<bool> IsVisibleChanged { get; set; }

    /// <summary>
    /// Extra class for the panel. <c>is-wide</c> relaxes the default reading width, for content that
    /// is a table or a diff rather than a question — see dashboard.css.
    /// </summary>
    [Parameter] public string? SizeClass { get; set; }

    private Task Close()
    {
        return IsVisibleChanged.InvokeAsync(false);
    }
}